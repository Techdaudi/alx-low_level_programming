#include <stdio.h>
#include <string.h>

/**
 * main - funtcion that prints string
 * @char1 - string to be printed.
 * Return: returns 0 if successful otherwise 1
 */

int main(void)
{
	/*Func description*/
	char char1[] = "with proper grammar, but the outcome is a piece of art,\n";

	printf("%s", char1);

	return (0);
}
